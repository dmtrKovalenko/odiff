
(library
 (name odiffIo)
 (flags
  (-w -40 -w +26))
  (foreign_stubs (language c) (names ReadPng))
  (c_library_flags -lpng16)
 )

(rule
 (targets c_flags.sexp c_library_flags.sexp)
 (action  (run ./config/discover.exe))
)
